# Copy to .env.local for local dev. Set the same keys in Vercel project settings for deploys.
# Nothing here is required yet; the app runs on mock data until the chain integration lands.
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
